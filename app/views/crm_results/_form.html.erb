<%= form_for(@crm_result) do |f| %>
  <% if @crm_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crm_result.errors.count, "error") %> prohibited this crm_result from being saved:</h2>

      <ul>
      <% @crm_result.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
