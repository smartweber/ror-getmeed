<p class="accordion lightbox-text"> Post Successful!
</p>
<hr class="hr-header">
<div class="form-view">
  <div class="lightbox-dialogue-info any-center">
    <p class="accordion">
      Posted to <strong><%= @school_list.length %></strong>
      <% if @school_list.length == 1%>
      school.
          <% else %>
      schools.
          <% end %>
      <br/>
      A Confirmation email has been sent to <strong><%= @job[:email] %></strong>! Use it for tracking applicants.
    </p>
  </div>
</div>